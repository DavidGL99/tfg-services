server:
  port: 8085

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DavidGL99/tfg-services
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASS}
          default-label: main

